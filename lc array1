class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hmap = {}
        for i,j in enumerate(nums):
            diff = target - j
            if diff in hmap:
                return [hmap[diff],i]
            else: hmap[j]=i
        return
